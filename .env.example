# Copy to .env and fill in. .env is gitignored.

# Gmail App Password (16-char, no spaces). Create at https://myaccount.google.com/apppasswords
# Requires 2FA enabled on the Google account.
GMAIL_APP_PASSWORD=

# Optional override for the From address (defaults to email.from_address in config.yaml)
# GMAIL_FROM_ADDRESS=
